# 오늘의 말씀 나눔 앱

동산감리교회 성도들이 **매일 같은 말씀을 읽고, 읽었음을 확인하고, 은혜를 나누는** 앱입니다.
빌드 도구 없이 정적 HTML로 동작하며, 두 가지 버전이 있습니다.

| 파일 | 내용 |
|---|---|
| `word.html` | 진입 화면 — 오늘의 말씀 미리보기 + 버전 선택 |
| `word-adult.html` | **성인용** — 본문 전문·묵상 길잡이·읽음 확인·나눔·현황·말씀표 |
| `word-kids.html` | **어린이용** — 쉬운 말씀·읽어주기·퀴즈·미션·도장/스티커판 |
| `word-data.js` | 공용 데이터 — 읽기 계획 + 큐레이션 말씀 67편 + 날짜 해석 유틸 |
| `word-core.js` | 공용 코어 — 저장소·사용자·확인·나눔·연속일수·성경 로더·공유 |

기존 `dongsan_bible.js`(새번역 59권)를 그대로 사용하며, 홈(`home.html`)과
교회 홈페이지(`index.html`) 상단 메뉴에서 바로 들어갈 수 있습니다.

## 오늘의 말씀은 이렇게 정해집니다

우선순위대로 적용되므로 **어떤 날에도 오늘의 말씀이 비지 않습니다.**

1. **인도자 지정** — 성인용 앱 › 설정 › “오늘의 말씀 직접 지정”에서 날짜·본문·제목을 입력한 경우
2. **교회 읽기표** — `WORD_PLAN`에 날짜가 있는 경우
   - **2026-08-17 ~ 10-15 · 열왕기하 60일 읽기** (`WORD_PLAN_2KINGS`) — 본문·제목과 함께
     성인용 묵상 질문·기도문, 어린이용 쉬운 설명·미션·퀴즈를 날짜마다 직접 담았습니다
   - 2026-03-02 ~ 04-29 · 사무엘상~하 (관리자 앱과 동일 데이터, 어린이 콘텐츠는 자동 구성)
3. **큐레이션 말씀** — 창세기부터 요한계시록까지 67편이 날짜 순번대로 순환
   (모든 기기에서 같은 날 같은 본문이 나오도록 날짜 기반으로 계산)

큐레이션 말씀에는 성인용 묵상 질문·기도문과 어린이용 쉬운 설명·미션·퀴즈가 함께 들어 있고,
읽기표 본문에는 어린이용 콘텐츠가 자동으로 구성됩니다.

## 성인용 기능

- 새번역 본문 전문(각주 기호 자동 정리), **글자 크기 4단계** — 어르신 배려
- 묵상 질문 + 한 줄 기도
- **읽음 확인** 한 번 누르면 연속 읽기(스트릭)·누적 일수 자동 집계, 취소 가능
- **은혜 나눔** 글 작성 → 성도들의 나눔 목록에 표시, `🙏 아멘` 반응, 내 글 삭제
- **현황** — 오늘 확인 인원, 최근 7일 나의 기록, 확인한 성도 목록, 연속 읽기 순위
- **말씀표** — 이번 달 전체 목록(읽은 날 ✓), 지난 날짜를 눌러 그날 말씀 다시 보기
- 카카오톡 공유(공유 API가 없으면 클립보드 복사), 알림 시간 설정, 기록 내보내기/가져오기

## 어린이용 기능

5단계로 진행합니다. 하루 5분이면 충분합니다.

1. **말씀 읽기** — 핵심 구절 큰 글씨, `🔊 읽어 주기`(기기 음성 합성), 전체 본문 펼쳐 보기
2. **이야기** — 아이 눈높이의 쉬운 설명
3. **말씀 퀴즈** — 3지선다, 정답이면 별 하나
4. **오늘의 미션** — 생활 속 실천 과제, 완료 시 별 하나
5. **마음 나누기** — 기분 이모지 + 한 줄 나눔 → 친구들 방에 표시(`👏 잘했어요`)

마지막으로 **도장 찍기**를 하면 그날의 읽기가 확정되고, **부모님 확인**을 남길 수 있습니다.
스티커판 탭에서 달력 도장·모은 별·연속 읽기·배지(🌱 🔥 🏅 👑 🌟)를 볼 수 있습니다.

## 매일 아침 5시 자동 게시

`.github/workflows/daily-word.yml` 이 **매일 아침 5시(한국시간)** 에 자동으로 실행되어
그날의 말씀 페이지를 만들고 저장소에 커밋합니다. GitHub Pages가 이를 그대로 배포합니다.

| 생성물 | 내용 |
|---|---|
| `today.html` | 오늘의 말씀 한 장 — 본문 전문 · 묵상 · 기도 · 어린이 코너 · 카톡 공유 · 인쇄 |
| `word-today.json` | 요약 데이터 — 교회 홈페이지(`index.html`) 상단 "오늘의 말씀" 카드가 읽어 씁니다 |

- 생성기: `node scripts/build-today.js [YYYY-MM-DD]` — 날짜를 주면 그날 것으로 만듭니다(미리보기·수동 게시용).
- 예약 시각은 cron `0 20 * * *`(UTC)입니다. 20:00 UTC = 다음 날 05:00 KST.
  GitHub 사정으로 몇 분 늦게 시작될 수 있습니다.
- 내용이 전날과 같으면 커밋하지 않고 넘어갑니다.
- 저장소 Actions 탭에서 **손으로 즉시 실행**(Run workflow)할 수도 있습니다.
- ⚠️ 예약 실행은 **기본 브랜치(master)에 있는 워크플로만** 동작합니다. 병합 전에는 자동 게시가 시작되지 않습니다.

## 설교말씀 (홈페이지 · 아카이브)

설교는 `sermons.json` 한 파일에서 관리하고, 교회 홈페이지 「설교 말씀」 섹션과
`archive.html` 이 이 파일을 함께 읽습니다.

- **카테고리는 성경 책 이름**입니다. 등록된 책만 탭으로 나타나며, 순서는 성경 차례를 따릅니다.
- 목록은 **날짜가 아니라 성경 차례(장·절 순)** 로 정렬됩니다. 화면에 날짜는 표시하지 않습니다.

한 편 등록:

```bash
node scripts/add-sermon.js \
  --scripture "로마서 8:31-39" \
  --title "끊을 수 없는 사랑" \
  --series "로마서 강해" \
  --summary "두세 문장 요약" \
  --points "1. 첫째 …|2. 둘째 …|3. 셋째 …" \
  --conclusion "맺는 말씀 …" \
  --hymns "찬송가 405장|찬송가 493장"
```

- 책 이름·장·절은 `--scripture` 에서 자동으로 뽑아 정렬과 카테고리에 씁니다.
- 같은 책의 같은 본문이 이미 있으면 덮어씁니다.
- `categories` 필드는 등록된 책으로 자동 갱신되므로 손댈 필요가 없습니다.

## 설교문 자동 게시

`sermons-src/` 폴더에 설교문을 올려 두면 **매일 아침 6시 30분(한국시간) 점검**이 돌면서
새 파일을 요약해 `sermons.json` 에 등록하고 커밋합니다. 자세한 안내는 `sermons-src/README.md` 참고.

- 읽을 수 있는 형식: `.txt` `.md` `.docx` (한글 `.hwp` 는 텍스트로 저장해 올리는 편이 확실합니다)
- 설교문 안이나 파일 이름에 **본문 장절**이 있어야 성경 책으로 분류됩니다. 없으면 등록하지 않고 물어봅니다.
- 이미 등록된 설교는 `scripture` 와 `source`(원본 파일명)로 걸러져 다시 올라가지 않습니다.
- 구글 드라이브 폴더도 함께 살피도록 되어 있으나, 예약 실행 세션에 드라이브 커넥터가 붙지 않는
  조직 설정이면 그 단계는 건너뜁니다. 드라이브를 쓰시려면 claude.ai 의 Routines 화면에서
  Google Drive 커넥터를 켠 Routine 을 직접 만들어 주셔야 합니다.

## 기록은 어디에 저장되나요

기본은 **이 기기(localStorage)** 입니다. 서버 없이도 앱 전체가 동작하며,
설정 화면에 “이 기기에만 저장됩니다”라고 그대로 표시됩니다.
다른 성도에게 전하려면 “카톡으로 나누기”를 사용합니다.

같은 도메인에 아래 API가 있으면 **자동으로 서버 공유 모드로 전환**되어,
성도들의 확인·나눔이 모든 기기에서 함께 보입니다. (앱 시작 시 `GET /api/word/feed`로 감지)

```
GET  /api/word/feed?date=YYYY-MM-DD   → {confirms:[...], shares:[...]}
POST /api/word/confirm                 {date,name,ver,quizOk,missionOk,mood,parent}
POST /api/word/confirm/delete          {date,name,ver}
POST /api/word/share                   {id,date,name,ver,text}
POST /api/word/share/delete            {id,name}
POST /api/word/amen                    {id,name}
```

`ver` 는 `adult` 또는 `kids` 입니다. 서버 호출이 실패해도 기기 기록으로 자동 대체되므로
오프라인에서도 앱이 멈추지 않습니다.

## 말씀 데이터 추가·수정

- 읽기표를 늘리려면 `word-data.js` 의 `WORD_PLAN` 에 `"2026-05-01": {passage,title,week_theme}` 형태로 추가합니다.
  묵상·어린이 콘텐츠까지 직접 쓰려면 `WORD_PLAN_2KINGS` 항목처럼 `kv/e/ks/m/q/c/a/qs/pr` 를 함께 넣으면 되고,
  넣지 않으면 제목을 바탕으로 자동 구성됩니다.
- 큐레이션 말씀은 `WORD_CYCLE` 배열에 항목을 추가하면 순환에 바로 포함됩니다.
  ```js
  {p:"본문", t:"제목", th:"주제", kv:"핵심절", e:"이모지",
   ks:"어린이 설명", m:"미션", q:"퀴즈", c:["보기1","보기2","보기3"], a:정답번호,
   qs:["묵상질문1","묵상질문2"], pr:"한 줄 기도"}
  ```
- 본문은 `dongsan_bible.js`(새번역)에서 읽어옵니다. 이 파일에 없는 7권
  (야고보서·베드로전후서·요한1~3서·유다서)은 “성경책에서 펴서 읽어요”로 안내됩니다.

## 알려진 본문 데이터 결함

`dongsan_bible.js` 에는 절 단위 누락이 있습니다. 앱은 이를 **조용히 건너뛰지 않고**
본문 아래에 “○절은 앱에 실린 본문 파일에 빠져 있습니다”라고 표시합니다(`WordBible.missing()`).

- **열왕기하: 25개 장 전체에서 12절이 없습니다.** 열왕기하 60일 읽기 중 25편이 여기에 해당합니다.
- 그 밖에도 여러 책의 313개 장에 누락된 절이 있습니다(예: 사무엘하 17:10, 역대상 1:13).

정확한 새번역 본문을 확보하면 `dongsan_bible.js` 를 교체하는 것이 근본 해결입니다.

---

## 설교문 자동 요약·등록 (2026-08-21 추가)

설교 원고를 올리면 요약까지 자동으로 만들어 홈페이지에 게시한다.

| 올리는 곳 | 흐름 |
|---|---|
| `sermons-src/` 폴더 (.docx/.txt/.md) | GitHub Actions 「설교문 자동 요약·등록」 → `scripts/import-sermons.js` → `sermons.json` → 홈페이지 |
| 관리자 화면 `admin.html` → 설교 탭 | 원고 붙여넣기 → 자동 요약 → JSON 복사·내려받기 (AI로 다듬기 선택 가능) |

- 요약 규칙은 `sermon-summary.js` 한 곳에 있고, 노드와 브라우저가 함께 쓴다.
- 원고의 짜임(제목 / 본문 / 서론 / 본론 / 결론 / 찬송)을 읽는 방식이라 API 키 없이 동작한다.
- `ANTHROPIC_API_KEY` (저장소 Secrets 또는 관리자 화면의 API Key)를 넣으면 AI 가 문장을 다듬는다.
- 같은 본문이 이미 등록되어 있으면 덮어쓰지 않는다 (`--force` 로만 교체).
- `.docx` 는 외부 패키지 없이 직접 읽는다 (ZIP → `word/document.xml`).

## 목회 시스템 정리 (2026-08-21)

- `deployed.html` (교인용 앱) 안에 구워져 있던 옛 설교 목록 50편을 삭제하고, 홈페이지와 같은
  `sermons.json` 을 읽어 **성경 책 이름 탭 · 날짜 없이** 표시하도록 바꿨다.
- 기기에 남아 있던 `dongsan_sermons` (localStorage) 는 앱을 열 때 한 번 지운다 (`dongsan_sermonReset` 표시).
- `admin.html` 에 **설교** 탭을 추가해 원고 붙여넣기 → 자동 요약 → 등록용 JSON 을 만들 수 있게 했다.
- 이로써 홈페이지 · 아카이브 · 교인용 앱 · 관리자 화면이 모두 `sermons.json` 한 파일을 본다.

## 설교 보관함 (2026-08-21)

예전 목회 시스템과 홈페이지에 올라와 있던 지난 설교 기록을 지우지 않고 따로 모아 두었다.

- `sermons-archive.json` — 지난 설교 69편 (2025-05-11 ~ 2026-12-13)
  - 옛 홈페이지 아카이브(말씀의 흐름·맺는 말씀까지 남아 있는 기록)와 옛 교인용 앱 목록을 합쳤고,
    같은 설교가 겹치면 내용이 더 많은 쪽을 남겼다.
  - 카테고리는 성경 책 이름, 목록은 날짜 역순(최근 설교가 위).
- `scripts/build-sermon-archive.js` — git 기록에서 옛 데이터를 꺼내 위 파일을 만든다. 한 번 만들면 다시 돌릴 일은 없다.
- `archive.html` 에 **🗄 설교 보관함** 탭을 더했다. 지금 진행 중인 강해(`sermons.json`)와는 목록이 섞이지 않는다.
  → **2026-08-21 이후 이 탭은 없어졌다.** 아래 「설교 보관함 암호 잠금」 참고.

| 파일 | 무엇 | 어디에 |
|---|---|---|
| `sermons.json` | 지금 진행 중인 강해 (출애굽기·로마서·빌립보서 58편) | 홈페이지 설교란 · 지난 말씀 「설교말씀」 · 교인용 앱 |
| `sermons-archive.enc` | 지난 설교 기록 69편 (암호로 잠금) | `vault.html` — 담임목사 전용 |
| `word-archive.json` | 오늘의 말씀 모음 84편 | 지난 말씀 「오늘의 말씀」 |

## 설교 본문 1000자 · 수요·금요 설교 정리 (2026-08-21)

- 목사님 요청으로 **수요예배 · 금요기도회 설교 28편을 sermons.json 에서 삭제**했다.
  원고는 구글 드라이브에 그대로 있고 git 기록에도 남아 있어 언제든 되돌릴 수 있다.
- 남은 **30편(출애굽기 17 · 로마서 1 · 빌립보서 12) 전부에 `body` 를 채웠다.**
  원고를 다시 읽고 1000자 안팎으로 정리한 설교 내용이며, 원고에 실제로 있는 내용만 썼고
  성도 개인 신상·건강 정보는 넣지 않았다.
- 화면: 「지난 말씀 보기」의 설교 카드를 누르면 **말씀(본문 1000자) → 말씀의 흐름 → 맺는 말씀 → 찬송** 순으로 펼쳐진다.
  홈페이지 카드에는 짧은 요약만 두고 전문은 아카이브에서 읽도록 안내한다.
- `scripts/set-body.js` — `{ "본문": "내용" }` 꼴 JSON 을 받아 본문을 채운다.
- `scripts/add-sermon.js --body "..."` 로도 등록할 수 있다.

## 설교 보관함 암호 잠금 (2026-08-21)

저장소가 공개(public)라 `sermons-archive.json` 을 홈페이지에 두면 주소만 알면 누구나 읽을 수 있었다.
그래서 보관함을 **담임목사 전용**으로 바꿨다.

- `archive.html` 의 **🗄 설교 보관함 탭을 없앴다.** 교인들이 보는 지난 말씀에는 설교말씀·오늘의 말씀 두 탭만 남는다.
- `vault.html` — 새로 만든 보관함 전용 페이지. 암호를 넣어야 열린다. 홈페이지 어디에서도 이 주소로 연결하지 않는다.
- `sermons-archive.enc` — 보관함을 통째로 암호로 잠근 파일. 주소를 직접 열어도 알아볼 수 없는 글자만 보인다.
  - AES-256-GCM · 암호에서 열쇠를 뽑을 때 PBKDF2-SHA256 25만 회.
  - 푸는 일은 **브라우저 안에서만** 일어난다. 암호는 어디로도 전송되지 않고 저장소에도 없다.
- `sermons-archive.json`(원본)은 홈페이지에서 내렸고 `.gitignore` 에 넣었다.
  필요하면 `node scripts/build-sermon-archive.js` 로 git 기록에서 언제든 다시 만들 수 있다.

### 암호 정하기 · 바꾸기

1. 저장소 **Settings → Secrets and variables → Actions → New repository secret**
   - 이름: `ARCHIVE_PASSPHRASE`  값: 쓰실 암호 (6자 이상)
2. **Actions** 탭 → **「설교 보관함 잠그기」** → **Run workflow**

돌리고 나면 `sermons-archive.enc` 가 새로 만들어진다. 암호를 바꾸고 싶으면 1번에서 값을 고치고 2번을 다시 돌리면 된다.

- `scripts/lock-archive.js` — 잠그는 일을 하는 스크립트. `ARCHIVE_PASSPHRASE` 환경변수로 암호를 받고 어디에도 적어 두지 않는다.
- `.github/workflows/lock-archive.yml` — 위 버튼이 돌리는 작업.

### 알아 두실 점

- **암호를 잊으면 보관함을 열 수 없다.** 다만 원본은 git 기록에 남아 있어 다시 만들어 새 암호로 잠글 수 있다.
- **2026-08-21 이전 기록에 올라갔던 원본은 저장소 기록(history)에 그대로 남아 있다.** 지금부터 새로 보는 사람은 잠긴 파일만 보지만, 예전 기록을 뒤지면 옛 내용을 찾을 수 있다. 이것까지 지우려면 저장소 기록을 다시 써야 한다.
- `vault.html` 은 검색엔진에 실리지 않도록 `noindex` 를 걸어 두었다.

## 사진첩 · 담임목사 인사말 사진 (2026-08-21)

- `photos.html` — 사진첩 페이지. 폴더별 카테고리, 크게 보기(좌우 넘기기·ESC 닫기).
- `photos/` — 사진을 넣는 곳. **폴더 하나가 카테고리 하나다.**
  처음 만들어 둔 폴더: `예배` · `교회학교` · `2026-보물캠프` · `봉사와-나눔` · `교회-풍경` · `담임목사`
- `scripts/build-photos.js` — `photos` 폴더를 훑어 `photos.json` 을 만든다.
  파일 이름이 사진 설명이 되고, 이름 앞의 `2026-07-21` 같은 날짜는 따로 뽑아 표시한다.
- `.github/workflows/photos.yml` — 사진을 올리면 위 목록을 자동으로 다시 만든다.
  목사님은 깃허브에서 폴더에 사진을 끌어다 놓기만 하시면 된다.

### 담임목사 인사말 사진

`photos/담임목사/인사말.jpg` 를 넣으면 교회소개의 인사말 자리에 들어간다.
파일이 없으면 지금처럼 '류' 글자가 그대로 보인다.

사진은 **연필 스케치**로 올린다. 브라우저에서 바꾸는 것이 아니라 **파일에 미리 입혀 둔다**.
홈페이지가 공개라 원본 사진을 올리면 누구나 내려받을 수 있기 때문이다. 이렇게 하면 올라가는 파일 자체가
스케치라 원본은 공개되지 않는다.

    node scripts/sketch-photo.js <원본> "photos/담임목사/인사말.jpg" 1400 9

흑백으로 바꾼 그림 위에 같은 그림을 반전·흐림 처리해 겹치면(color dodge) 면은 하얗게 날아가고
윤곽선만 남아 연필 그림이 된다. 마지막 두 숫자는 가로 크기와 번짐 정도다.

가로로 넓은 사진(단체사진 등)이면 인사말 카드가 저절로 위아래 배치로 바뀌어 사진이 잘리지 않는다.
(`.pastor-wrap.wide-photo`)

현재 올라간 사진: 2026 보물캠프 단체사진 (1400x788, 287KB)

### 사진 올릴 때 유의할 점

사진첩은 누구나 볼 수 있는 공개 홈페이지다. 어린이·청소년이 나온 사진은 가정의 동의를 받은 것만 올린다.
자세한 안내는 `photos/README.md` 에 적어 두었다.

## 보관함 잠그기 오류 수정 (2026-08-22)

첫 실행이 실패했다. 두 가지 문제가 있었다.

1. **보관함이 69편 → 20편으로 줄었다.** `scripts/build-sermon-archive.js` 는 git 기록을 훑어
   보관함을 다시 만드는데, PR 을 스쿼시 병합하면서 원본 커밋(`2ef0d9e`)이 사라져 절반 넘게
   복원되지 않았다. 그대로 잠갔다면 설교 49편이 조용히 없어졌을 것이다.
   → 이제 기록을 훑지 않고, 보관함 69편이 담긴 커밋(`e55da9c`, master 에 살아 있음)에서
     그대로 꺼내 쓴다. 그리고 **60편 미만이면 잠그지 않고 멈춘다.**
2. **게시 단계가 exit 128 로 죽었다.** `git add -A sermons-archive.enc sermons-archive.json`
   에서 원본은 잠근 뒤 지워지고 `.gitignore` 에도 있어 pathspec 이 맞지 않았다.
   → `sermons-archive.enc` 만 add 한다.

두 번째 문제 덕분에 첫 번째 문제가 커밋되지 않았다.

## 설교 보관함·잠금장치 삭제 · 관리자 비밀번호 정리 (2026-08-22)

### 보관함을 없앴다

목사님 요청으로 설교 보관함과 암호 잠금을 통째로 걷어냈다.

- `vault.html` — 삭제
- `scripts/lock-archive.js` · `.github/workflows/lock-archive.yml` — 삭제
- `scripts/build-sermon-archive.js` — 삭제
- `archive.html` 의 보관함 카드 — 삭제
- `.gitignore` — 보관함 원본만 막던 파일이라 삭제

**되살려야 할 때**: 지난 설교 69편은 git 기록에 그대로 있다.

    git show e55da9c46d9249eae73520a0d5efae4f1b880e8d:sermons-archive.json > sermons-archive.json

깃허브에 남아 있는 `ARCHIVE_PASSPHRASE` 시크릿은 이제 쓰지 않으므로 지워도 된다.
(Settings 에서 Secrets and variables 에 있는 Actions 항목)

### 관리자 비밀번호는 원래 동작하지 않았다

홈 화면에 있던 두 버튼(오늘의 말씀 작성 / 예배시간·인사말·연락처 수정)은
`/api/admin/verify` 로 비밀번호를 확인하고 `/api/dailywords` 로 저장하는 구조였다.
그런데 이 저장소에는 **서버가 없다** (`.env`·`package.json`·서버 코드 모두 없음).
GitHub Pages 정적 호스팅이라 `/api/…` 는 존재할 수 없다.

즉 비밀번호를 무엇으로 넣든 "서버에 연결할 수 없습니다"만 떴다. 안내에 적혀 있던
`.env` 의 `CHURCH_PASSWORD` 도 실재하지 않는 파일이다. 예전 서버 기반 홈페이지에서
넘어온 잔재였다.

그래서 눌러도 오류만 나는 버튼·모달·서버 호출 코드를 모두 걷어냈다.
자주 바뀌는 정보(예배시간·인사말·연락처)는 지금처럼 이 파일에 직접 적어 게시한다.
